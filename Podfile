# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'add2app-host' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for add2app-host

  target 'add2app-hostTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'add2app-hostUITests' do
    # Pods for testing
  end

end
